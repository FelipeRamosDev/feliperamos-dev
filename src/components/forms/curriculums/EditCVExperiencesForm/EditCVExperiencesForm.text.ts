import { TextResources } from '@/services';

const textResources = new TextResources();

textResources.create('CVExperiences.submitButton', 'Save Changes', 'en');
textResources.create('CVExperiences.submitButton', 'Salvar Alterações', 'pt');

export default textResources;
