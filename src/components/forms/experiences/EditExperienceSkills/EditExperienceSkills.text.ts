import { TextResources } from '@/services';

const textResources = new TextResources();

textResources.create('EditExperienceSkills.submit.label', 'Save Changes');
textResources.create('EditExperienceSkills.submit.label', 'Salvar Alterações', 'pt');

export default textResources;

