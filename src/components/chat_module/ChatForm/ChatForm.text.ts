import { TextResources } from '@/services';

const chatFormText = new TextResources();

chatFormText.create('ChatForm.buttonTitle.send', 'Send Message');
chatFormText.create('ChatForm.buttonTitle.send', 'Enviar Mensagem', 'pt-BR');

export default chatFormText;
